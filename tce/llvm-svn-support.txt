tce-devel r1873:
llvm-trunk 180591:
* new -lcpp parameter need to be added when compiling/linking. C++ code.
* false positives(on short systemtests):
  * some andn operation matched twice instead of once
  * ALU8 has more adds than the addresses ones.
