/**
 * Transitional file for backwards compatibility to older TCE versions.
 *
 * New code should include tce_config.h directly because config.h does not
 * get installed.
 */
#include "tce_config.h"
