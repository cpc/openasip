/**
 * @file Memory.icc
 *
 * Inline definitions of Memory class.
 *
 * @author Pekka J‰‰skel‰inen 2004 (pekka.jaaskelainen@tut.fi)
 * @note rating: red
 */


/**
 * Returns true if the memory is idle, that is, there's no
 * pending write request waiting to be commited.
 *
 * @return True if memory is idle.
 */
inline bool
Memory::isIdle() const {
    return idle_;
}
